import React from 'react';

export const Loading: React.FC = () => {
  return (
    <span className='flex justify-center items-center w-full min-h-screen'>
      <p>Loading...</p>
    </span>
  );
};

